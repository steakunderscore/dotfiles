# Set your name and email with the following:
#   git config -f ~/.gitconfig.local user.name "John Appleseed"
#   git config -f ~/.gitconfig.local user.email me@example.com

[include]
  path = ~/.gitconfig.local
[color]
  ui = auto
[core]
  editor = vim
  excludesfile = ~/.gitignore
[git-up "bundler"]
  check = true
[rebase]
  autosquash = true
[color "diff"]
  commit = yellow reverse
[diff]
  renames = true
[push]
  default = current
[fetch]
  prune = true
[init]
  templatedir = ~/.git_template
[merge]
  log = true
  conflictstyle = diff3
  tool = vimdiff
[mergetool]
  prompt = false
[alias]
  co = checkout
  p = push
  ci = commit
  rb = rebase
  rc = rebase --continue
  rs = rebase --skip
  br = branch -vva
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  rp = remote prune
  statsu = status
  st = status
  scrub = clean -dxf
  push = push --recurse-submodules=check
  hunt = log --after='48 hours ago' --author
  work = "!sh -c 'git checkout -b bug/$0 origin/master'"
  ignore = "!gi() { curl https://www.gitignore.io/api/$@ > .gitignore ;}; gi"
[branch]
  autosetuprebase = always
[grep]
  linenumber = true
[pager]
  log = diff-highlight | less
  show = diff-highlight | less
  diff = diff-highlight | less
